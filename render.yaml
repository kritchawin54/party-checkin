services:
  - type: web
    name: party-checkin
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx tsx server.mjs
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "22"
